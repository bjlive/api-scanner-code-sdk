package com.example.multifile.crossref.constants;

/**
 * Resource-name constants (independent of version).
 * Combined with Versions in controller annotations.
 */
public interface ResourcePaths {
    String BILLING  = "/billing";
    String ACCOUNTS = "/accounts";
}
